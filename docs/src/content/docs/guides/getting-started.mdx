---
title: Getting Started
description: Learn how to install and set up starlight-page-action in your Starlight project.
---

## Prerequisites

You need an existing [Starlight](https://starlight.astro.build/) project. If you don't have one yet, follow the [Starlight Getting Started guide](https://starlight.astro.build/getting-started/) to create one.

## Installation

Install the plugin using your preferred package manager:

```bash
npm install starlight-page-action
```

```bash
pnpm add starlight-page-action
```

```bash
yarn add starlight-page-action
```

## Setup

Add the plugin to your Starlight configuration in `astro.config.mjs`:

```js
import { defineConfig } from "astro/config";
import starlight from "@astrojs/starlight";
import starlightPageAction from "starlight-page-action";

export default defineConfig({
  integrations: [
    starlight({
      title: "My Docs",
      plugins: [starlightPageAction()],
    }),
  ],
});
```

That's it! The plugin will automatically:

1. Add a **"Copy page"** button that copies the raw markdown of the current page
2. Add an **"Ask AI"** dropdown with links to ChatGPT, Claude, and T3 Chat
3. Place both actions in the right sidebar above the table of contents

## Verifying the Setup

Start your dev server and look at the right sidebar. You should see a **"Page Actions"** section above the table of contents with the copy and AI buttons.

```bash
npm run dev
```

## Next Steps

- [Configure](/guides/configuration/) which actions to show and customize the AI prompt.
- See [examples](/examples/default/) of different configurations.
